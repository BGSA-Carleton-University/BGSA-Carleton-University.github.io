This is the start of the read me file for the BGSA website.
•	Start by installing hugo via Winget
First go to terminal, then enter `winget install Hugo.Hugo.Extended` for Windows. Otherwise, follow instructions at this link for Mac/Linux : https://gohugo.io/installation/
•	A previous collaborator needs to add you as a collaborator on this website
•	Pushes and pulls can be done in R Studio. Make sure R is connected (Check notes from Val’s lecture to make sure you know how to do this)

•	Changes can be made in 
•	Go into terminal and type hugo to get it to deploy the website

In the terminal use `git submodule update --init --recursive` to load in the submodules. 

In the terminal use `hugo serve` to check a local version of the website for pushed changes.